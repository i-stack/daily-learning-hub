import {readdir,readFile,writeFile,mkdir} from 'node:fs/promises';
const root=new URL('../',import.meta.url);const dataDir=new URL('data/yfj/',root);const files=(await readdir(dataDir)).filter(x=>/^YFJ-D\d{3}\.json$/.test(x)).sort();const courses=[];for(const f of files)courses.push(JSON.parse(await readFile(new URL(f,dataDir),'utf8')));await writeFile(new URL('index.json',dataDir),JSON.stringify(courses,null,2)+'\n');
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const layout=(title,path,body)=>`<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="每日阴符经：从原文到现代实践的结构化学习系统"><title>${esc(title)}｜每日阴符经</title><link rel="stylesheet" href="${path}assets/styles.css"></head><body><header class="shell site-head"><a class="brand" href="${path}index.html">每日阴符经</a><nav class="head-links" aria-label="主导航"><a href="${path}weekly/index.html">周索引</a><a href="${path}monthly/index.html">知识树</a></nav></header>${body}<footer><div class="shell">以结构化数据为唯一事实源 · 每日一课，观天执行</div></footer></body></html>`;
const home=layout('首页','',`<main><section class="shell hero"><div><div class="eyebrow">Daily Study · Classics</div><h1>观天之道<br>执天之行</h1><p class="hero-copy">一套可持续生长的《阴符经》学习系统。每天从一句原文出发，完成理解、关联、实践与验收。</p></div><div class="seal">阴符<br>日课</div></section><section class="shell stats"><div class="stat"><b data-current>—</b><span>当前进度</span></div><div class="stat"><b data-done>—</b><span>已完成课程</span></div><div class="stat"><b data-total>—</b><span>课程总数</span></div></section><section class="shell workspace"><div class="tools"><h2>课程目录</h2><input class="search" data-search type="search" placeholder="搜索标题、章节或关键词" aria-label="搜索课程"></div><div class="course-list" data-course-list><div class="empty">正在读取课程数据…</div></div></section></main><script src="./assets/app.js" defer></script>`);await writeFile(new URL('index.html',root),home);
await mkdir(new URL('days/',root),{recursive:true});const labels=[['original','01 原文'],['explanation','02 逐句白话'],['coreIdea','03 核心思想'],['connections','04 经典关联'],['applications','05 现实映射'],['reflection','06 今日思考'],['questions','07 知识验收'],['mastery','08 掌握标准']];for(let i=0;i<courses.length;i++){const c=courses[i],prev=courses[i-1],next=courses[i+1];const sections=labels.map(([key,label])=>{const v=c.sections[key];const content=Array.isArray(v)?`<ol>${v.map(x=>`<li>${esc(x)}</li>`).join('')}</ol>`:`<p class="${key==='original'?'original':''}">${esc(v)}</p>`;return `<section class="lesson-section" id="${key}"><h2>${label}</h2>${content}</section>`}).join('');const toc=labels.map(([key,label])=>`<a href="#${key}">${label}</a>`).join('');const nav=`<nav class="lesson-nav">${prev?`<a href="${prev.id}.html">← ${esc(prev.title)}</a>`:'<span></span>'}<a href="../index.html">返回主目录</a>${next?`<a href="${next.id}.html">${esc(next.title)} →</a>`:'<span></span>'}</nav>`;const body=`<main class="shell"><div class="breadcrumb"><a href="../index.html">典籍</a> › <a href="../index.html">阴符经</a> › <a href="../index.html">主目录</a> › ${c.id}</div><header class="entry-head"><div class="eyebrow">${c.id}</div><h1>${esc(c.title)}</h1><div class="meta">${esc(c.chapter)} · ${esc(c.date)} · ${c.keywords.map(esc).join(' / ')}</div></header><div class="entry-grid"><aside class="toc" aria-label="本课目录">${toc}</aside><article><p class="hero-copy">${esc(c.summary)}</p>${sections}${nav}</article></div></main>`;await writeFile(new URL(`days/${c.id}.html`,root),layout(c.title,'../',body));}
await mkdir(new URL('weekly/',root),{recursive:true});await mkdir(new URL('monthly/',root),{recursive:true});const weeks=[];for(let i=0;i<courses.length;i+=7)weeks.push(courses.slice(i,i+7));const weekly=`<main class="shell"><div class="breadcrumb"><a href="../index.html">主目录</a> › 周索引</div><header class="entry-head"><div class="eyebrow">Weekly Review</div><h1>周索引</h1></header><div class="hub-grid">${weeks.map((w,i)=>`<section class="hub-card"><div class="eyebrow">Week ${i+1}</div><h2>${w[0].id}—${w.at(-1).id}</h2>${w.map(c=>`<p><a href="../days/${c.id}.html">${c.id}｜${esc(c.title)}</a></p>`).join('')}</section>`).join('')}</div></main>`;await writeFile(new URL('weekly/index.html',root),layout('周索引','../',weekly));const groups=Map.groupBy(courses,c=>c.chapter);const monthly=`<main class="shell"><div class="breadcrumb"><a href="../index.html">主目录</a> › 月度知识树</div><header class="entry-head"><div class="eyebrow">Knowledge Tree</div><h1>月度知识树</h1><p class="hero-copy">按篇章与关键词聚合，随着日课持续生长。</p></header><div class="hub-grid">${[...groups].map(([chapter,items])=>`<section class="hub-card"><div class="eyebrow">${items.length} 课</div><h2>${esc(chapter)}</h2><p>${[...new Set(items.flatMap(x=>x.keywords))].map(esc).join(' · ')}</p>${items.map(c=>`<p><a href="../days/${c.id}.html">${c.id}｜${esc(c.title)}</a></p>`).join('')}</section>`).join('')}</div></main>`;await writeFile(new URL('monthly/index.html',root),layout('月度知识树','../',monthly));console.log(`Built ${courses.length} lessons.`);
